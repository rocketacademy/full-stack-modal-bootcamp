<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
  </head>
  <body>
    <h4>Items:</h4>
    <% items.forEach( item => { %>
      <p>
        <%= item.id %> : <%= item.name %>
      </p>
    <% }) %>
  </body>
</html>
